# Example environment variables for local development and tests
# Copy this file to `.env` in the app you'd like to run and update values if necessary

# MySQL (mariadb driver expects mariadb://...)
MYSQL_CONNECTION=mariadb://evdb:evdbpassword@localhost:3306/evdb_test

# Postgres
POSTGRES_CONNECTION=postgres://evdb:evdbpassword@localhost:5432/evdb_test

# Local DynamoDB
DYNAMODB_CONNECTION=http://localhost:8000
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local
AWS_REGION=us-east-1

# Notes:
# - Start the local DBs with: `docker compose up -d`
# - MySQL and Postgres DBs may need initialization/migrations before use.
# - This file is safe to commit; do NOT commit your real credentials — use .env for private values.
